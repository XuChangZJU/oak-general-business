
export default OakComponent({
    entity() {
        const { entity } = this.props;
        return entity;
    },
    isList: false,
    actions() {
        const { actions } = this.props;
        return actions
    },
    methods: {
    },
});
